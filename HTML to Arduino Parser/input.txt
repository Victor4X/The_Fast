document.onkeydown = function(evt) {
    evt = evt || window.event;
    if (evt.key == "w") {
	moveForward()
    }
    if (evt.key == "a") {
	moveLeft()
    }
    if (evt.key == "s") {
	moveBackward()
    }
    if (evt.key == "d") {
	moveRight()
    }
    
};